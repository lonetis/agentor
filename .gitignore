# Dependencies
node_modules/

# Nuxt build output
.nuxt/
.output/

# Environment
.env

# Legacy credential directory (no longer used by the orchestrator — per-user
# credentials are stored under data/users/<userId>/credentials/ — but keep
# the pattern so installations upgrading from the old layout don't
# accidentally commit leftover local files)
.cred/

# Persistent data (environments, per-user credentials, etc.)
data/

# Playwright
tests/playwright-report/
tests/test-results/
tests/.auth/

# Vitest coverage
orchestrator/coverage/

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
*.swo
